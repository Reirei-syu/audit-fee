# Project Specification

## 系统边界

- 当前项目为纯前端本地工具，主入口为 `index.html`。
- 现有运行依赖 `vendor/` 中的浏览器端脚本。
- 本次工作目标是生成一个离线、单文件、可直接打开的 HTML 成品。

## 技术约束

- 优先采用最小改动原则，不重写现有 React/Babel 浏览器运行模式。
- 不引入新的运行时依赖。
- 打包过程应通过本地脚本完成，便于重复执行。

## 架构约束

- UI / 页面交互保留在 `index.html`。
- 打包逻辑独立为脚本，不与业务计算逻辑耦合。
- 成品输出目录固定为 `D:\coding\completed\audit-fee`。

## 当前交付目标

1. 仓库补齐基础协作文档。
2. 新增单文件打包脚本。
3. 生成可直接双击打开的单文件成品。
4. 完成本地冒烟验证。

